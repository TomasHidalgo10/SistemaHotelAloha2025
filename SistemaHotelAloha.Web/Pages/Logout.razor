@page "/logout"
@inject AuthService Auth
@inject NavigationManager Nav

<p>Cerrando sesión...</p>

@code {
    protected override void OnInitialized()
    {
        Auth.Logout();
        Nav.NavigateTo("/");
    }
}
